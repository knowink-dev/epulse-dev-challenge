<div class="nested-fields bg-light border-2">
  <div class="form-group">
    <%= f.association :election_candidate, include_blank: "Select Candidate", class: 'form-control', collection: ElectionCandidate.all.map {|ec| [ec.full_name, ec.id]} %>
  </div>
  <div class="p-1">
    <%= link_to_remove_association "Remove Candidate Enrollment", f , data: {confirm: "Are you sure???"}, class: 'btn btn-sm btn-danger' %>
  </div>
</div>